id: dufs
name: dufs
alias: dufs
type: binary
description: "Dumb file server with static serving, uploading, searching, access control and WebDAV"
tags: [network, file, server, transfer]
github: sigoden/dufs
homepage: https://github.com/sigoden/dufs
binary_name: dufs
arch_map:
  amd64:
    asset_pattern: "x86_64-unknown-linux-musl.tar.gz"
    binary_path: "dufs"
  arm64:
    asset_pattern: "aarch64-unknown-linux-musl.tar.gz"
    binary_path: "dufs"
  armv7:
    asset_pattern: "armv7-unknown-linux-musleabihf.tar.gz"
    binary_path: "dufs"
